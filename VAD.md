# VAD 優化與推論調校結案報告

## 1. 專案背景

本文件記錄了解決 EdgeMeetingSDK 中 Whisper ASR **推論延遲過長 (20s)**、**幻覺 (Hallucination)** 以及 **音訊遺失 (Missing Audio)** 問題的完整過程與最終解決方案。

## 2. 問題分析

### 2.1 延遲過長 (Latency Issue)

- **現象**：使用者停止說話後，系統沒有立即進行轉錄，而是持續錄音直到達到 20 秒緩衝區上限才強制觸發。
- **根因**：環境中的高頻底噪（Hiss noise）導致 Zero Crossing Rate (ZCR) 偏高 (~0.20)。原有的 VAD 閾值 (`PAUSE_ZCR_THRESHOLD = 0.06`) 過於嚴格，將底噪誤判為 `SPEECH`，導致靜音計時器不斷被重置。

### 2.2 幻覺問題 (Hallucination)

- **現象**：在安靜環境下，模型會輸出 "(bells chiming)", "(static)", 或 "字幕由...提供" 等無意義內容。
- **根因**：Whisper 模型是一種 Sequence-to-Sequence 模型，當輸入為純靜音或無語義的雜訊時，Decoder 容易產生不穩定的輸出（幻覺）。
- **解決**：實作「語音密度檢測」，若一段音訊中有效語音長度不足 500ms，直接在 C++ 層丟棄，不進行推論。

### 2.3 音訊遺失/空字串 (Signal Level Issue)

- **現象**：VAD 顯示 `SPEECH` 狀態，日誌顯示推論已觸發，但輸出的轉錄文字為空 (`''`)，導致使用者感覺「話被吃掉了」。
- **根因**：硬體麥克風的輸入增益過低 (RMS ~0.01 / Int16 ~300)。雖然這足以觸發 VAD（基於相對變化），但低於 Whisper 模型的有效動態範圍（模型預期約 -3dBFS），導致模型將其視為背景噪音過濾掉。

## 3. 最終實作參數 (As-Built Configuration)

### 3.1 VAD 閾值 (WhisperAsrEngine.cpp)

針對 RK3588 平台與特定麥克風特性的最終調校值：


| 參數                      | 舊值     | 新值        | 說明                        |
| ----------------------- | ------ | --------- | ------------------------- |
| `SILENCE_RMS_THRESHOLD` | 800.0  | **40.0**  | 低於此值強制視為靜音，忽略 ZCR (抗底噪關鍵) |
| `PAUSE_RMS_THRESHOLD`   | 1500.0 | **150.0** | 修正為符合實際麥克風電平的範圍           |
| `PAUSE_ZCR_THRESHOLD`   | 0.06   | **0.25**  | **關鍵修正**：容忍高頻嘶嘶聲，避免誤判為語音  |


### 3.2 訊號處理

- **數位增益 (Digital Gain)**：**10.0x** (在推論前 Pre-process 階段套用)
- **截幅保護 (Clamping)**：限制在 `[-1.0, 1.0]` 區間。

### 3.3 邏輯策略

1. **VAD 狀態機**：
  - `SILENCE` (RMS < 40)
  - `PAUSE` (RMS < 150 && ZCR < 0.25)
  - `SPEECH` (其他)
2. **推論觸發條件**：
  - Buffer >= 2.0s
  - State 為 `SILENCE` 或 `PAUSE`
  - 靜音累積 >= 500ms
3. **防幻覺過濾**：
  - **必須滿足**：`speechDurationMs >= 500ms` (Buffer 內必須包含至少 0.5秒 的有效語音)，否則丟棄。

## 4. Lesson Learned (經驗總結)

1. **VAD 閾值不能依賴通用標準**：
  - 不同硬體、麥克風、甚至驅動層的增益設定都會巨大地影響 RMS 和 ZCR。
  - **教訓**：必須先收集實際設備的 Log (`RMS`, `ZCR`)，觀察底噪分佈，再設定閾值。不能盲目使用開源專案的預設值。
2. **ZCR 是雙面刃**：
  - ZCR 對於區分「人聲」和「低頻噪音（風聲）」很有效，但在電子設備常見的「高頻嘶嘶聲（Hiss）」面前容易失效。
  - **教訓**：需要加入「低能量強制靜音」邏輯（`if RMS < Threshold then Force Silence`），在能量極低時忽略 ZCR，防止底噪干擾。
3. **模型是「聾」的，不是「壞」的**：
  - 當 VAD 正常但不出字時，第一直覺往往是懷疑模型壞了或被截斷。但常被忽略的是 **Input Level**。
  - Whisper 是在標準化音量下訓練的。如果輸入太小，它會什麼都聽不到。
  - **教訓**：在 Debug ASR 時，務必監控送入模型的 PCM 數值範圍 (Amplitude)。
4. **延遲感往往來自於「切不斷」**：
  - 使用者覺得「慢」，很多時候不是推論慢（RTF 通常很快），而是系統「不知道你講完了沒」，一直在等一個完美的靜音。
  - **教訓**：放寬 `PAUSE` 的判定標準，比追求完美的 `SILENCE` 對使用者體驗更重要。

## 5. 未來建議

- **Auto Gain Control (AGC)**：目前使用固定的 10x 增益。未來建議在 `AudioProcessor` 層級加入動態 AGC，以適應不同講話音量。
- **Noise Suppression (NS)**：目前的 VAD 是基於統計特徵。若環境更吵雜，建議引入 WebRTC 的 NS 模組進行降噪，再做 VAD。